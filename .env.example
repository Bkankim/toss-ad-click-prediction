# 데이터 루트 경로
DATA_ROOT=/Competition/CTR/toss-ad-click-prediction/data

# 학습/검증에 사용할 기본 데이터 파일 경로
TRAIN_PATH=${DATA_ROOT}/train.parquet
TEST_PATH=${DATA_ROOT}/test.parquet

# 캐시 및 임시 산출물 경로
CACHE_DIR=/Competition/CTR/cache
SUBMISSION_DIR=/Competition/CTR/toss-ad-click-prediction/submission

# 실험 로깅 옵션
EXPERIMENT_TRACKING=mlflow
MLFLOW_TRACKING_URI=http://localhost:5000

# LightGBM 기본 난수 시드
GLOBAL_SEED=42

# GPU 사용 여부 (0: 미사용, 1: 사용)
USE_GPU=1

# 환경별 변수 추가 시 아래에 작성하세요.
